import styled from "styled-components";
import {Tooltip} from "antd";

const TooltipStyled = styled(Tooltip)`
  font-family: Raleway, sans-serif;
  font-size: 8px;
`

export {TooltipStyled as Tooltip};