import styled from "styled-components";

const AddElementContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1.4em;
`

const AddElementTitle = styled.p`
  color: black;
  font-size: 16px;

`
export {AddElementContainer, AddElementTitle};